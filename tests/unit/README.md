# Unit test placeholder for decision-engine
# Tests will be added alongside Go application logic

## Decision Engine (Go)
Tests go in `decision-engine/*_test.go` files.

## ML Sidecar (Python)
Tests go in `ml-sidecar/tests/` directory.
Run with: `pytest ml-sidecar/tests/`

## Lua / OpenResty
Tests go in `tests/unit/lua/` directory.
Run with: `busted tests/unit/lua/`
